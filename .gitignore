node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
*.local

# Base de données locale libSQL (bind-mount, ne jamais committer)
/data/*.db
/data/*.db-*

# Certificats locaux (mkcert)
/certs/*.pem

# Données importées volumineuses (régénérables via les scripts data:*)
/static/data/*.geojson
/static/data/*.json
!/static/data/.gitkeep

.DS_Store
